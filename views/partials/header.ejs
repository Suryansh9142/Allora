<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="Content-Security-Policy" content="default-src 'self'; font-src 'self' https://fonts.gstatic.com https://cdnjs.cloudflare.com data:; img-src 'self' data: https: blob:; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com https://cdnjs.cloudflare.com; script-src 'self' 'unsafe-inline'; connect-src 'self' https://fonts.gstatic.com https://cdnjs.cloudflare.com;">
    
    <!-- Remove preload links that were causing warnings -->
    
    <!-- Preconnect to Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" crossorigin>
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    
    <!-- Font stylesheets with crossorigin attribute -->
    <link href="https://fonts.googleapis.com/css2?family=Lobster&display=swap" rel="stylesheet" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css2?family=Caveat:wght@400..700&family=Lobster&display=swap" rel="stylesheet" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css2?family=Macondo&display=swap" rel="stylesheet" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css2?family=Caveat&family=Cinzel+Decorative:wght@400;700;900&family=Macondo&display=swap" rel="stylesheet" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/styles.css">
    <title><%= typeof title !== 'undefined' ? title + ' - Allora' : 'Allora - Your Entertainment Hub' %></title>
    
    <!-- Inline font definitions as fallback -->
    <style>
        /* Self-hosted font fallbacks */
        @font-face {
            font-family: 'Lobster';
            font-style: normal;
            font-weight: 400;
            font-display: swap;
            src: local('Lobster'), url(/fonts/lobster.woff2) format('woff2');
        }
        
        @font-face {
            font-family: 'Caveat';
            font-style: normal;
            font-weight: 400;
            font-display: swap;
            src: local('Caveat'), url(/fonts/caveat.woff2) format('woff2');
        }
        
        /* Fallback system font stack */
        body {
            font-family: 'Lobster', 'Caveat', 'Arial', sans-serif;
        }
        
        /* Card Link Styles */
        .card-link {
            display: block;
            height: 100%;
            color: inherit;
            text-decoration: none;
        }

        /* Detail Page Styles */
        .detail-page {
            padding: 0;
            width: 100%;
            min-height: calc(100vh - 200px); /* Account for header and footer */
            display: flex;
            justify-content: center;
        }

        .detail-container {
            width: 100%;
            max-width: 100%;
            margin: 0;
            background-color: rgba(20, 20, 30, 0.9);
            box-shadow: 0 0 30px rgba(0, 0, 0, 0.7);
        }

        .detail-header {
            display: flex;
            align-items: center;
            padding: 25px 40px;
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            border-bottom: 2px solid #2a2a4a;
            width: 100%;
            position: sticky;
            top: 0;
            z-index: 10;
        }

        .detail-header h1 {
            color: white;
            font-size: 32px;
            margin: 0;
            flex-grow: 1;
        }

        .back-button {
            display: inline-flex;
            align-items: center;
            color: #b8b8d4;
            text-decoration: none;
            font-size: 16px;
            margin-right: 20px;
            transition: color 0.3s ease;
        }

        .back-button:hover {
            color: #ff5e62;
        }

        .back-button i {
            margin-right: 5px;
        }

        .detail-content {
            display: flex;
            padding: 40px;
            width: 100%;
            gap: 40px;
            align-items: flex-start;
        }

        .detail-poster {
            width: 350px;
            flex-shrink: 0;
            position: sticky;
            top: 100px;
        }

        .detail-poster img {
            width: 100%;
            border-radius: 15px;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.5);
            transition: transform 0.3s ease;
        }

        .detail-poster img:hover {
            transform: scale(1.02);
        }

        .detail-rating {
            margin: 15px 0;
            font-size: 20px;
            text-align: center;
        }

        .detail-actions {
            display: flex;
            flex-direction: column;
            gap: 10px;
            margin-top: 20px;
        }

        .detail-actions .btn {
            width: 100%;
        }

        .detail-info {
            flex-grow: 1;
            width: 100%;
            max-width: calc(100% - 390px); /* Account for poster width + gap */
        }

        .detail-meta {
            display: flex;
            flex-wrap: wrap;
            gap: 15px;
            margin-bottom: 30px;
        }

        .detail-meta span {
            background-color: rgba(255, 94, 98, 0.15);
            color: #ff5e62;
            padding: 8px 16px;
            border-radius: 25px;
            font-size: 14px;
            font-weight: 500;
            backdrop-filter: blur(5px);
            transition: all 0.3s ease;
        }

        .detail-meta span:hover {
            background-color: rgba(255, 94, 98, 0.25);
            transform: translateY(-2px);
        }

        .detail-description {
            margin-bottom: 30px;
            width: 100%;
        }

        .detail-description h3 {
            color: white;
            font-size: 20px;
            margin-bottom: 10px;
        }

        .detail-description p {
            color: #b8b8d4;
            line-height: 1.6;
        }

        .detail-genres {
            margin-bottom: 30px;
            width: 100%;
        }

        .detail-genres h3 {
            color: white;
            font-size: 20px;
            margin-bottom: 10px;
        }

        .detail-cast {
            margin-bottom: 30px;
            width: 100%;
        }

        .detail-cast h3 {
            color: white;
            font-size: 20px;
            margin-bottom: 10px;
        }

        .cast-list {
            list-style: none;
            padding: 0;
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
        }

        .cast-list li {
            background-color: rgba(255, 255, 255, 0.1);
            color: white;
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 14px;
        }

        .detail-section {
            padding: 40px;
            border-top: 2px solid #2a2a4a;
            width: 100%;
            background-color: rgba(26, 26, 46, 0.8);
        }

        .detail-section h2 {
            color: white;
            font-size: 28px;
            margin-bottom: 30px;
            position: relative;
            padding-left: 15px;
        }

        .detail-section h2::before {
            content: '';
            position: absolute;
            left: 0;
            top: 50%;
            transform: translateY(-50%);
            width: 4px;
            height: 70%;
            background: #ff5e62;
            border-radius: 2px;
        }

        .info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 25px;
            width: 100%;
        }

        .info-item {
            background-color: rgba(40, 40, 60, 0.7);
            border-radius: 12px;
            padding: 20px;
            transition: all 0.3s ease;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .info-item:hover {
            transform: translateY(-3px);
            background-color: rgba(50, 50, 70, 0.7);
            border-color: rgba(255, 94, 98, 0.3);
        }

        .info-item h4 {
            color: #ff5e62;
            font-size: 16px;
            margin-bottom: 5px;
        }

        .info-item p {
            color: white;
            font-size: 14px;
        }

        .download-options {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
            gap: 25px;
            margin-bottom: 30px;
            width: 100%;
        }

        .download-option {
            background-color: rgba(40, 40, 60, 0.7);
            border-radius: 12px;
            padding: 25px;
            display: flex;
            flex-direction: column;
            transition: all 0.3s ease;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .download-option:hover {
            transform: translateY(-3px);
            background-color: rgba(50, 50, 70, 0.7);
            border-color: rgba(255, 94, 98, 0.3);
        }

        .download-info {
            flex-grow: 1;
            margin-bottom: 15px;
        }

        .download-info h4 {
            color: white;
            font-size: 18px;
            margin-bottom: 10px;
        }

        .download-info p {
            color: #b8b8d4;
            font-size: 14px;
            margin-bottom: 5px;
        }

        .download-btn {
            align-self: flex-start;
            padding: 12px 24px;
            border-radius: 25px;
            font-weight: 500;
            transition: all 0.3s ease;
            width: auto;
        }

        .download-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(255, 94, 98, 0.3);
        }

        .download-note {
            display: flex;
            align-items: flex-start;
            background-color: rgba(255, 94, 98, 0.1);
            border-radius: 12px;
            padding: 20px;
            margin-top: 30px;
            width: 100%;
            border: 1px solid rgba(255, 94, 98, 0.2);
        }

        .download-note i {
            color: #ff5e62;
            font-size: 20px;
            margin-right: 10px;
            margin-top: 2px;
        }

        .download-note p {
            color: #b8b8d4;
            font-size: 14px;
            line-height: 1.5;
        }

        .related-content {
            margin-top: 20px;
            width: 100%;
        }

        /* Responsive styles for detail page */
        @media (max-width: 1200px) {
            .detail-content {
                padding: 30px;
                gap: 30px;
            }

            .detail-poster {
                width: 300px;
            }

            .detail-info {
                max-width: calc(100% - 330px);
            }

            .info-grid, .download-options {
                grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            }
        }

        @media (max-width: 992px) {
            .detail-content {
                flex-direction: column;
            }

            .detail-poster {
                width: 100%;
                max-width: 400px;
                margin: 0 auto;
                position: relative;
                top: 0;
            }

            .detail-info {
                max-width: 100%;
            }
        }

        @media (max-width: 768px) {
            .detail-header {
                padding: 20px;
            }

            .detail-content, .detail-section {
                padding: 20px;
            }

            .detail-header h1 {
                font-size: 24px;
            }

            .info-grid, .download-options {
                grid-template-columns: 1fr;
                gap: 15px;
            }

            .download-option {
                padding: 20px;
            }
        }

        @media (max-width: 480px) {
            .detail-meta {
                gap: 10px;
            }

            .detail-meta span {
                padding: 6px 12px;
                font-size: 12px;
            }

            .download-btn {
                width: 100%;
                text-align: center;
            }
        }

        /* Genres Page Styles */
        .genres-container {
            /* max-width: 1200px; */
            margin: 2rem 0;
            padding: 0 1rem;
            background-color: rgba(0, 0, 0, 0.7);
        }

        .section-title {
            text-align: start;
            margin-bottom: 2rem;
        }

        .section-title h2 {
            font-size: 2.5rem;
            margin-bottom: 1.5rem;
            color: #fff;
        }

        .section-title p {
            font-size: 1.2rem;
            color: #ccc;
        }

        .search-container {
            position: relative;
            max-width: 500px;
            margin: 0 auto 2rem;
        }

        .search-container input {
            width: 100%;
            padding: 12px 20px;
            padding-right: 50px;
            border-radius: 30px;
            border: none;
            background-color: rgba(255, 255, 255, 0.1);
            color: white;
            font-size: 1rem;
            transition: all 0.3s ease;
        }

        .search-container input:focus {
            outline: none;
            background-color: rgba(255, 255, 255, 0.15);
            box-shadow: 0 0 0 2px rgba(229, 9, 20, 0.5);
        }

        .search-container .search-icon {
            position: absolute;
            right: 20px;
            top: 50%;
            transform: translateY(-50%);
            color: #ccc;
            font-size: 1.2rem;
        }

        .genre-card-grid {
            display: grid;
            grid-template-columns:repeat(auto-fill, minmax(442px, 1fr));
            gap: 2rem;
        }

        .genre-card {
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            background-color: rgba(30, 30, 40, 0.8);
        }

        .genre-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.4);
        }

        .genre-card-link {
            text-decoration: none;
            color: inherit;
            display: block;
        }

        .genre-card-image {
            position: relative;
            height: 180px;
            overflow: hidden;
        }

        .genre-card-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s ease;
        }

        .genre-card:hover .genre-card-image img {
            transform: scale(1.1);
        }

        .genre-overlay {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(to top, rgba(0, 0, 0, 0.8) 0%, transparent 100%);
        }

        .genre-card-content {
            padding: 1.5rem;
            text-align: center;
        }

        .genre-card-content h3 {
            font-size: 1.5rem;
            margin-bottom: 0.5rem;
            color: #fff;
        }

        .genre-count {
            display: inline-block;
            background-color: #e50914;
            color: white;
            padding: 0.3rem 0.8rem;
            border-radius: 20px;
            font-size: 0.9rem;
        }

        /* Genre Detail Page Styles */
        .genre-detail-container {
            /* max-width: 1200px; */
            /* margin: 2rem auto; */
            padding: 0 1rem;
        }

        .genre-header {
            display: flex;
            align-items: center;
            margin-bottom: 2rem;
        }

        .back-button {
            display: inline-flex;
            align-items: center;
            color: #fff;
            text-decoration: none;
            font-size: 1rem;
            margin-right: 2rem;
            transition: color 0.3s ease;
        }

        .back-button:hover {
            color: #e50914;
        }

        .back-button i {
            margin-right: 0.5rem;
        }

        .genre-header h1 {
            font-size: 2.5rem;
            color: #fff;
        }

        .genre-content {
            margin-top: 2rem;
        }

        .card-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 2rem;
            margin-top: 2rem;
        }

        .card {
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
            transition: transform 0.3s ease, box-shadow 0.3s ease, border-color 0.3s ease;
            background-color: rgba(30, 30, 40, 0.8);
            border: 1px solid rgba(255, 255, 255, 0.1);
            position: relative;
            display: flex;
            flex-direction: column;
        }

        .card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.5);
            border-color: rgba(255, 94, 98, 0.3);
        }

        .card img {
            width: 100%;
            height: 300px;
            object-fit: contain;
            background-color: #111;
            transition: transform 0.5s ease, filter 0.5s ease;
            height: 420px;
        }

        .card:hover img {
            transform: scale(1.05);
            filter: brightness(1.1);
        }
        
        .card::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(to bottom, rgba(0, 0, 0, 0.8) 0%, transparent 50%, rgba(0, 0, 0, 0.8) 100%);
            opacity: 0;
            transition: opacity 0.3s ease;
            pointer-events: none;
        }

        .card:hover::after {
            opacity: 1;
        }

        .card-content {
            padding: 20px;
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            background-color: rgba(20, 20, 30, 0.9);
        }

        .card-content h3 {
            font-size: 1.2rem;
            margin-bottom: 8px;
            color: #fff;
            font-weight: 600;
            line-height: 1.4;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .card-content .rating {
            color: #f9c22e;
            margin-bottom: 8px;
            font-size: 1.2rem;
            letter-spacing: 2px;
        }

        .card-content p {
            color: #b8b8d4;
            font-size: 0.9rem;
            margin-top: 8px;
            line-height: 1.5;
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .card-type {
            display: inline-block;
            background-color: #333;
            color: white;
            padding: 0.2rem 0.5rem;
            border-radius: 4px;
            font-size: 0.8rem;
            margin: 0.5rem 0;
        }

        /* Responsive Styles */
        @media (max-width: 768px) {
            .genre-card-grid {
                grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
                gap: 1.5rem;
            }

            .genre-header {
                flex-direction: column;
                align-items: flex-start;
            }

            .back-button {
                margin-bottom: 1rem;
            }
            
            .card img {
                width: 100%;
                height: 200px;
            }
            
            .card-content {
                padding: 15px;
            }
            
            .card-content h3 {
                font-size: 1rem;
                margin-bottom: 5px;
            }
            
            .card-content .rating {
                font-size: 1rem;
                margin-bottom: 5px;
            }
            
            .card-content p {
                font-size: 0.85rem;
                margin-top: 5px;
                -webkit-line-clamp: 2;
            }
        }

        @media (max-width: 480px) {
            .genre-card-grid {
                grid-template-columns: 1fr;
            }
            
            .card img {
                width: 100%;
                height: 180px;
            }
            
            .card-content {
                padding: 10px;
            }
            
            .card-content h3 {
                font-size: 0.9rem;
                margin-bottom: 3px;
            }
            
            .card-content .rating {
                font-size: 0.9rem;
                margin-bottom: 3px;
            }
            
            .card-content p {
                font-size: 0.8rem;
                margin-top: 3px;
                -webkit-line-clamp: 2;
            }
        }
    </style>
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Montserrat:wght@400;500;600;700&display=swap" rel="stylesheet" crossorigin="anonymous">
</head>
<body class="<%= typeof bgClass !== 'undefined' ? bgClass : '' %>">
    <!-- Navbar Section -->
    <section id="navbar">
        <!-- Hamburger Menu Button -->
        <div class="hamburger-menu">
            <div class="bar"></div>
            <div class="bar"></div>
            <div class="bar"></div>
        </div>
        
        <ul class="nav-links">
            <li><a href="/">Home</a></li>
            <li><a href="/anime">Anime</a></li>
            <li><a href="/movies">Movies</a></li>
            <li><a href="/tvshows">TV Shows</a></li>
            <li><a href="/genres">Genres</a></li>
        </ul>
    </section>

    <section id="heading">
        <div id="heading-1">
            <%= typeof heading !== 'undefined' ? heading : '' %>
        </div>
    </section>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Handle image loading errors
            document.querySelectorAll('img').forEach(img => {
                img.setAttribute('crossorigin', 'anonymous');
            });
        });
    </script>
</body>
</html> 